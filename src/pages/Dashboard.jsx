// Estat en temps real de totes les zones via WebSocket (/ws/status)
// TODO: connectar wsClient i mostrar humitat, temperatura, botó reg manual

export default function Dashboard() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-green-800 mb-4">Dashboard</h1>
      <p className="text-gray-500">Estat en temps real — pendent d'implementació</p>
    </div>
  )
}
